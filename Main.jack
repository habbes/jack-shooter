class Main {
    function void main () {
        do Hero.draw(0);
        return;
    }

    function void fillScreen (boolean color) {
        do Screen.setColor(color);
        do Screen.drawRectangle(0, 0, 511, 255);
        return;
    }
}