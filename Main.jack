class Main {
    function void main () {
        var Game game;
        let game = Game.new();
        do game.run();        
        return;
    }

    function void fillScreen (boolean color) {
        do Screen.setColor(color);
        do Screen.drawRectangle(0, 0, 511, 240);
        return;
    }
}